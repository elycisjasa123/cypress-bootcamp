{
  "name": "cypress-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open_please": "npx cypress open --config specPattern=./cypress/e2e/Login.spec.cy.js --browser chrome",
    "cypress:run_please": "npx cypress run --config specPattern=./cypress/e2e/Login.spec.cy.js --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.9.0"
  }
}
